name: Render Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          echo "Deployment triggered by push to main."
          echo "Render will automatically deploy via Git integration."
          echo "See Render dashboard for live deployment status."