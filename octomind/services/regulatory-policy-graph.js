/**
 * RegulatoryPolicyGraph
 *
 * Builds and queries a graph of regulatory policies and controls.
 * (Gold Nugget #1)
 */

class RegulatoryPolicyGraph {
  query(policy) {
    return { relevantControls: [] };
  }
}

module.exports = RegulatoryPolicyGraph;
