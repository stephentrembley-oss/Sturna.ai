/**
 * ConstitutionalFilter
 *
 * Applies constitutional / ethical filters to agent outputs.
 */

class ConstitutionalFilter {
  filter(output) {
    return output;
  }
}

module.exports = ConstitutionalFilter;
